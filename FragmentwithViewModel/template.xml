<?xml version="1.0"?>
<template
    format="5"
    revision="1"
    name="Fragment with ViewModel"
    description="Creates a fragment and viewmodel">

    <category value="TT Custom" />

    <parameter
        id="fragmentName"
        name="Fragment Name"
        type="string"
        constraints="class|nonempty|unique"
        default="BlankFragment"
        help="The name of the fragment class to create" />

    <parameter
        id="fragmentLayoutName"
        name="Fragment Layout Name"
        type="string"
        constraints="layout|nonempty|unique"
        default="fragment_blank"
        suggest="fragment_${classToResource(fragmentName)}"
        help="The name of the layout to create" />

    <parameter
        id="viewModelName"
        name="ViewModel Name"
        type="string"
        constraints="class|nonempty|unique"
        default="BlankFragmentViewModel"
        suggest="${fragmentName}ViewModel"
        help="The name of the viewmodel to create" />

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
